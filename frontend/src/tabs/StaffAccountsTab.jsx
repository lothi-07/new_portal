import { useState } from 'react'
import { createStaffAccount } from '../api'
export default function StaffAccountsTab() {
  const [form, setForm] = useState({ name: '', email: '', password: '' }); const [saving, setSaving] = useState(false)
  const submit = async () => { if (!form.email || !form.password) return alert('Email and password are required.'); setSaving(true); try { await createStaffAccount(form); setForm({ name: '', email: '', password: '' }); alert('Staff account created. The staff member can now sign in and publish event flyers.') } catch (e) { alert(e.response?.data?.detail || 'Unable to create staff account') } finally { setSaving(false) } }
  return <div style={s.wrap}><h1 style={s.title}>Staff Accounts</h1><p style={s.subtitle}>Staff can publish event flyers but cannot access student records or other administrative tools.</p><div style={s.card}><input style={s.input} placeholder="Staff name" value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /><input style={s.input} type="email" placeholder="Staff email *" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /><input style={s.input} type="password" placeholder="Temporary password *" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} /><button style={s.button} onClick={submit} disabled={saving}>{saving ? 'Creating...' : 'Create Staff Account'}</button></div></div>
}
const s = { wrap: { animation: 'fadeIn .3s ease' }, title: { margin: 0, color: '#1a2469', fontSize: 22, fontWeight: 800 }, subtitle: { margin: '6px 0 18px', color: '#666', fontSize: 13.5, maxWidth: 650 }, card: { background: '#fff', padding: 22, borderRadius: 12, boxShadow: '0 1px 6px rgba(0,0,0,.06)', maxWidth: 440 }, input: { display: 'block', boxSizing: 'border-box', width: '100%', marginBottom: 12, padding: '10px 12px', border: '1.5px solid #ddd', borderRadius: 7, fontSize: 13 }, button: { padding: '10px 16px', border: 'none', borderRadius: 7, background: '#1a2469', color: '#fff', fontWeight: 600 } }
